// import React from 'react'
// import DataLoader from '../DataLoader/DataLoader';

// 		const Loader = () => (
// 			<React.Fragment>
// 				{Array(30)
// 				.fill("")
// 				.map((e, i) => (
// 					<DataLoader key={i} style={{ opacity: Number(3 / i).toFixed(1) }} />
// 				))}
// 			</React.Fragment>
//             )
            
//   export default Loader